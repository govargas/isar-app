-- ================================================
-- FIX SWEDISH CHARACTERS - STEP 1: GET IDS
-- Run this first and share the results
-- ================================================

SELECT id, name, region FROM lakes ORDER BY id;
